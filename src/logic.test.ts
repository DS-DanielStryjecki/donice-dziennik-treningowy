import{describe,it,expect}from'vitest';import{nextChestVariant,recommend,targetFor,volume}from'./logic';import type{Recovery,TrainingSession}from'./types';
const now=new Date('2026-07-11T12:00:00Z');const s=(focus:TrainingSession['focus'],variant:string,status:TrainingSession['status']='full',date='2026-07-10T10:00:00Z'):TrainingSession=>({id:Math.random()+'',templateId:'x',name:`${focus} ${variant}`,focus,variant,startedAt:date,completedAt:date,status,exercises:[]});const r=(x:Partial<Recovery>={}):Recovery=>({id:'r',createdAt:now.toISOString(),sleep:4,energy:4,chestSoreness:2,shoulderSoreness:2,backSoreness:2,jointPain:1,note:'',...x});
describe('rotacja i rekomendacje',()=>{it('pełna klatka przesuwa rotację',()=>expect(nextChestVariant([s('Klatka','A')])).toBe('B'));it('lekka klatka nie przesuwa rotacji',()=>expect(nextChestVariant([s('Klatka','A','light')])).toBe('A'));it('po ciężkiej klatce A nie poleca barków A',()=>expect(recommend([s('Klatka','A')],r(),now).template.id).toBe('back-a'));it('po lekkiej klatce poleca Barki B przy zdrowych barkach',()=>expect(recommend([s('Klatka','A','light')],r(),now).template.id).toBe('shoulders-b'));it('ból stawów >4 kieruje na regenerację',()=>expect(recommend([],r({jointPain:5}),now).template.id).toBe('recovery'));it('obolała klatka nie dostaje klatki',()=>expect(recommend([s('Plecy','A','full','2026-07-09T10:00:00Z')],r({chestSoreness:7}),now).template.focus).not.toBe('Klatka'));it('cel zwiększa powtórzenia',()=>expect(targetFor({weight:80,reps:8,rir:2}).reps).toBe(9));it('liczy objętość roboczą',()=>expect(volume({...s('Klatka','A'),exercises:[{exerciseId:'x',notes:'',sets:[{id:'1',kind:'working',weight:80,reps:10,rir:2,done:true}]}]})).toBe(800))});
