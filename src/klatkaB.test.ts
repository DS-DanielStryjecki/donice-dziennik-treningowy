import{describe,it,expect}from'vitest';import{exercises,templates}from'./data';
describe('import KLATKA B',()=>{it('zachowuje identyfikator planu',()=>expect(templates.find(x=>x.variant==='B'&&x.focus==='Klatka')?.id).toBe('chest-b-hypertrophy-cables'));it('zachowuje identyfikatory i zalecenia ćwiczeń',()=>{const e=exercises.find(x=>x.id==='incline-dumbbell-press');expect(e?.namePl).toBe('Wyciskanie hantli na ławce dodatniej');expect(e?.sets).toBe(4);expect(e?.repRange).toEqual({min:8,max:12});expect(e?.targetRir).toBe('1–2');expect(e?.restSeconds).toBe(150);expect(e?.tempo).toBe('3-0-1-0')});it('zachowuje instrukcje błędy i wskazówki',()=>{const e=exercises.find(x=>x.id==='pec-deck-fly');expect(e?.steps.length).toBeGreaterThan(2);expect(e?.errors.length).toBeGreaterThan(2);expect(e?.coachCues?.length).toBeGreaterThan(1)});it('przypisuje zweryfikowane grafiki do ćwiczeń',()=>{expect(exercises.find(x=>x.id==='incline-dumbbell-press')?.imageUrl).toBe('/images/exercises/incline-dumbbell-press.webp');expect(exercises.find(x=>x.id==='cable-fly')?.imageUrl).toBe('/images/exercises/cable-fly.webp');expect(exercises.find(x=>x.id==='rope-pushdown')?.imageUrl).toBe('/images/exercises/rope-pushdown.webp')})});
